version: '3.8'

services:
  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
    environment:
      MINIO_ROOT_USER: root
      MINIO_ROOT_PASSWORD: 1234
    volumes:
      - minio_data:/data
    command: server /data

volumes:
  minio_data:
